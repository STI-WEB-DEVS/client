# TODO

- [ ] Review existing Customers management page and Product management page implementation.
- [ ] Create a Products management page that is fully similar to Customers page (table + Add modal + View modal + Delete) using `ProductService`.
- [ ] Ensure Products page calls backend for CRUD: `getAll`, `create`, `update` (if used), `deleteProduct`.
- [ ] Fix current Products page issues (missing modal UI / unused edit state).
- [ ] Run a quick Nuxt build/lint (if available) to ensure no TypeScript/Vue errors.

